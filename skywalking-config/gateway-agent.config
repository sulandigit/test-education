# SkyWalking Agent 网关服务配置
# 网关服务名称
agent.service_name=${SW_AGENT_NAME:education-gateway}

# 后端服务地址
collector.backend_service=${SW_AGENT_COLLECTOR_BACKEND_SERVICES:oap-server:11800}

# 日志级别
logging.level=${SW_LOGGING_LEVEL:INFO}

# 采样率配置 - 网关作为入口，采样率可以稍高
agent.sample_n_per_3_secs=${SW_AGENT_SAMPLE:5}

# Spring Cloud Gateway 特殊配置
plugin.spring.cloud.gateway.collect_http_params=${SW_SPRING_CLOUD_GATEWAY_COLLECT_HTTP_PARAMS:true}

# 忽略健康检查等接口
agent.ignore_suffix=${SW_AGENT_IGNORE_SUFFIX:.jpg,.jpeg,.js,.css,.png,.bmp,.gif,.ico,.mp3,.mp4,.html,.svg,/actuator/health,/actuator/info}

# 认证信息
agent.authentication=${SW_AGENT_AUTHENTICATION:""}

# 操作名称最大长度
agent.operation_name_threshold=${SW_AGENT_OPERATION_NAME_THRESHOLD:150}